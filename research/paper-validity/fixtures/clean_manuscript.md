# Abstract
We compared two arms under a pre-registered design. Accuracy in arm A was
<?reg acc.A ?> and in arm B was <?reg acc.B ?>. The difference was
<?reg diff.point ?> (95% CI <?reg diff.ci ?>), and the equivalence margin
was fixed at <?reg delta ?> before data acquisition.

# Methods
The corpus contained <?reg corpus.n ?> papers. Power at the registered worst
point of the discordance curve was <?reg power.worst ?>.
The reported upper limit was <?reg fwer.ub ?>.

# Results
Runtime fell by <?reg speed.ratio ?> relative to the baseline.

# References
