# R13 — v5.2 독립 공격 4기 결과 (3차)
| 기 | FATAL | MAJOR | MINOR | 핵심 |
|---|---|---|---|---|
| A 통계 | 0 | 4 | 1 | "크기" 미정의(2×차), (n,k) 최소가 파레토 경계, ICC=1에서 부록 D 발산; 부록 D 미지정 선택 4개; 실시 술어에 `corpus.n` 부재; 재시도 단위 불명; 열등/유의 부기 라벨 무특성 |
| B 독립성 | 0 | 4 | 1 | 준비 단계 네트워크 무규제(인덱스에 봉입 가능); **역할 겸임 허용 → 정답 산출자=B11 구현자 가능**; `dev.acc.lo`가 실현 베이스라인을 묶지 않음; 해시 대조는 바이트 동일 사본만 |
| C 회귀 | **2** | 3 | — | **L13 항진명제·화이트리스트 자기적합 시연(E23)**; §7 상호 독립성 삭제(FATAL); 재시도 축소+게이트 삭제로 팔 의존 인프라 실패가 주 종점 직입(FATAL); 오염 적격화는 명칭 면제; 부록 D가 Fréchet 집합 실현 불가 |
| D 실행 | 0 | 4 | 1 | 비수치 정답이 SCORABLE에 남아 전 칸 0점; "응답 토큰 0"이 4개 로거마다 다름; `dev.pass`/`dev.acc.lo` 게이트 무특성·실패 분기 없음; 라벨러 입력 무허용목록; 수치 문법이 등록 해시 밖 |
**중복 제거: FATAL 2 · MAJOR 14.** → **v5.2 ≤ 80.** v4·v5.0·v5.1·v5.2 네 버전 연속.

## R29 판정 (R13-C)
- 술어 ①③ 삭제(R12-C 처방)는 **틀렸다** — v5.1에서 유일하게 작동특성이 등록된 게이트였다.
- L13(R12 후 REG-011 처방)은 **항진명제** — 화이트리스트 13개 문구 중 4개가 v5.2에만 존재(`이상으로 등록`은 v5.2 §3 한 문장에서만). R23 위반.
- **두 라운드의 회귀 사냥꾼이 반대 처방을 냈고 나는 둘 다 따랐다.** → R31.

## v5.3 처분
충돌 없는 제약만: §7 배타 분할 복원 · 준비 격리 · 봉인 수탁자≠중재자 · 크기 정의 = P(L>δ|Δ=δ) ≤ α/2 · (n,k) 사전식 + `runs.max` · 실시 술어에 m ≥ `corpus.n` · `elig.lo` PW-0 편입 · 부록 D 선택 명시(μ 재해석·가우스 코퓰라·논문 단위 희석·격자) + Fréchet 부분 실현 선언 · 부록 D·E·`norm.hash`·`harness.hash` 등록 해시 편입 · T3 정답 스칼라 제한 · `tol.abs` · 부기 라벨 삭제 · 바이트 동일 감도 선언 · 빌드별 청구서.
**동결**(R31): 0-fail 게이트·재시도 조건·오염 게이트 — 두 독법을 §11에 병기.
**L13 재작성**: 비교 연산자 근접 + 부록 E 등록표 대조. 기지결함 픽스처(`fixtures/l13_defect.md`)로 R23 검증 — validate_gates [5].
**점수 없음.** 4차 공격 전에는 v5.3의 점수를 말하지 않는다.
